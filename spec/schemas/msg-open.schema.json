{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gtme.spec/schemas/msg-open.schema.json",
  "title": "OPEN (runner → adapter)",
  "description": "First message of every adapter session (SPEC.md §5). Derived from internal/protocol.Message: every field but `type` carries `omitempty`, so an OPEN with an empty config omits the key entirely.",
  "type": "object",
  "required": ["type"],
  "additionalProperties": false,
  "properties": {
    "type": {"const": "OPEN"},
    "step_id": {"type": "string", "description": "The pipeline step id this session serves."},
    "run_id": {"type": "string", "description": "ULID of the run."},
    "config": {"type": "object", "description": "The step's resolved `with:` block, validated against the manifest's config_schema before it is sent."},
    "pending": {
      "type": "object",
      "required": ["token"],
      "additionalProperties": false,
      "properties": {
        "token": {"type": "string", "minLength": 1}
      },
      "description": "ADR-038. Present only when the runner is collecting: the token the adapter emitted in a PENDING message. The adapter MUST NOT dispatch new work; it collects results for the records that follow, or emits PENDING again if they are not ready."
    },
    "preflight": {
      "type": "boolean",
      "description": "ADR-040. Present (true) only in a preflight session: no records follow, and the adapter answers PREFLIGHT then END after checking the live target against this step's config (its variables: targets ride in config as ever)."
    }
  }
}
