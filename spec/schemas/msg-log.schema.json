{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gtme.spec/schemas/msg-log.schema.json",
  "title": "LOG (adapter → runner)",
  "description": "Human-facing noise from an adapter (SPEC.md §5). The runner relays it to stderr; stdout is reserved for data (SPEC.md §8).",
  "type": "object",
  "required": ["type", "level", "msg"],
  "additionalProperties": false,
  "properties": {
    "type": {"const": "LOG"},
    "level": {"enum": ["info", "warn", "error"]},
    "msg": {"type": "string"}
  }
}
